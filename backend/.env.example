# Copy this file to .env only if a dotenv loader is added.
# For the current project, provide these values as environment variables when starting Node.
PORT=3001
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-this-password
HF_API_TOKEN=hf_replace_with_your_token
